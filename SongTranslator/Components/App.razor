<!DOCTYPE html>
<html lang="en">


<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <base href="/" />
    <link rel="stylesheet" href="bootstrap/bootstrap.min.css" />
    <link href="~/app.css" rel="stylesheet" />
    <link rel="icon" type="image/png" href="favicon.png" />
    <HeadOutlet />
</head>

<body>
    <Routes />
    <script src="_framework/blazor.web.js"></script>
</body>

<style>
    @@import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap');

    body {
        font-family: 'Inter', sans-serif;
        background: #0f0f10;
        color: #f9f9f9;
        margin: 0;
        padding: 0;
    }

    .video-lyrics-container {
        display: flex;
        width: 100%;
        justify-content: space-evenly;
        align-items: center;
        margin-top: 10vh;
    }

    .single-video-box-placeholder,
    .lyrics-container,
    .lyrics-box-placeholder {
        width: 45%;
        max-width: 640px;
        aspect-ratio: 16 / 9;
        /* background-color: #f2f2f2; */
        background: linear-gradient(90deg, #fdceff, #d5b0f3);
        /* border: 3px dashed #e687e9; */
        color: #966ca2;
        font-size: 1.4rem;
        display: flex;
        justify-content: center;
        align-items: center;
        border-radius: 2vh;
        padding: 1.5vh;
        animation: fadeInUp 2s ease forwards;
    }

    .lyrics-container {
        width: 45%;
        max-width: 640px;
        max-height: 360px;
        aspect-ratio: 16 / 9;
        background-color: #f2f2f2;
        border: 3px dashed #e687e9;
        color: #555;
        font-size: 1.2rem;
        padding: 1.5vh 2vw;
        animation: fadeInUp 2s ease forwards;
        display: block;
        white-space: pre-wrap;
        word-wrap: break-word;
        line-height: 1.5;
        text-align: left;
        font-family: sans-serif;
        overflow-y: auto;
        border-radius: 2vh 0vh 0vh 2vh;
    }

    .single-video-box {
        opacity: 0;
        transform: translateY(-20px);
        animation: fadeInUp 2s ease forwards;
    }

    @@keyframes fadeInUp {
        to

    {
        opacity: 1;
        transform: translateY(0);
    }

    }

    /* Responsive Design */
    @@media (max-width: 768px) {
        .video-lyrics-container

    {
        flex-direction: column;
        justify-content: center;
        align-items: center;
        margin-top: 5vh;
    }

    .single-video-box-placeholder,
    .lyrics-container,
    .lyrics-box-placeholder {
        width: 90%;
        max-width: 100%;
        min-width: unset;
        min-height: 200px;
        max-height: none;
        font-size: 1rem;
        padding: 15px;
    }

    .lyrics-container,
    .lyrics-box-placeholder {
        margin-top: 20px;
    }

    }


    .input-group {
        display: flex;
        justify-content: center;
        gap: 1rem;
        padding: 4vh 2vw;
    }

    .input-group input {
        background: rgba(255,255,255,0.08);
        border: 1px solid rgba(255,255,255,0.1);
        color: #fff;
        padding: 0.8rem 1rem;
        width: 200px;
        transition: 0.3s ease;
    }

    .input-group .searchButtonForFetching {
        background: linear-gradient(90deg, #e687e9, #a95de5);
        border: none;
        color: white;
        font-weight: 600;
        padding: 2vh;
        border-radius: 2vh;
        transition: 0.3s ease;
    }

    .input-group .searchButtonForFetching:hover {
        transform: scale(1.05);
        background: linear-gradient(90deg, #a95de5, #e687e9);
    }


    .recommended-videos {
        display: flex;
        flex-direction: row;
        align-items: flex-start;
        /* gap: 3vh; */
        overflow-x: auto;
        scroll-behavior: smooth;
        padding: 4vh 5vh;
        background: #6c3a6d;
        /* border-radius: 5vh 5vh 0 0; */
        box-shadow: 0 -2px 5px rgb(0 0 0 / 20%);
        width: -webkit-fill-available;
        justify-content: space-evenly;
        flex-wrap: wrap;
    }

     
    .recommendedVideoCard {
        flex: 0 0 auto;
        width: 22vh;
        transition: transform 0.3s ease, box-shadow 0.3s ease;
        border-radius: 1.5vh;
        overflow: hidden;
        background: #1f1f1f;
    }

    .recommendedVideoCard:hover {
        transform: translateY(-0.8vh) scale(1.02);
    }

    .recommendedVideoCardFigure {
        margin: 0;
        display: flex;
        flex-direction: column;
        min-height: 28vh;
        max-height: 28vh;
        min-width: fit-content;
        max-width: 40vh;
        overflow-x: auto;
        overflow-y: auto;
        align-items: center;
    }

    .recommendedVideoThumb {
        width: 100%;
        height: 13vh;
        object-fit: cover;
        border-bottom: 2px solid rgba(230, 135, 233, 0.4);
        /* transition: opacity 0.3s ease; */
    }

    .recommendedVideoTitle {
        font-size: 1.8vh;
        text-align: justify;
        padding: 1vh;
        color: #e687e9;
        font-weight: 500;
        letter-spacing: 0.3px;
        transition: color 0.3s ease;
    }

    .recommendedVideoCard:hover .recommendedVideoTitle {
        color: #f1b6f3;
    }


    .display-recommendations {
        display: flex;
        align-items: center;
        justify-content: flex-start;
        margin: 5vh 5vh 2vh 5vh;
        position: relative;
    }

    .display-recommendations h3 {
        font-size: 3vh;
        font-weight: 700;
        letter-spacing: 1px;
        background: linear-gradient(90deg, #e687e9, #f5a9f7, #e687e9);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        text-shadow: 0 0 15px rgba(230, 135, 233, 0.4);
        position: relative;
    }

    .display-recommendations::after {
        content: "";
        position: absolute;
        bottom: -0.8vh;
        left: 0;
        width: 80px;
        height: 3px;
        border-radius: 3px;
        background: linear-gradient(90deg, #e687e9, transparent);
        box-shadow: 0 0 10px rgba(230, 135, 233, 0.4);
    }


    .fetchedVideoAndLyrics{
        width: 100%;
        background: #121212;
        padding: 5vh;
        border-radius: 2vh;
        box-shadow: 0 0 30px rgba(230, 135, 233, 0.1);
        color: #fff;
        font-family: 'Inter', sans-serif;
        display: flex;
        flex-direction: column;
        align-items: center;
    }

    .lyrics-toolbar {
        display: flex;
        gap: 2vh;
        margin-bottom: 4vh;
    }

    .toggle-btn {
        background: linear-gradient(90deg, #e687e9, #f5a9f7);
        border: none;
        color: #121212;
        font-weight: 600;
        border-radius: 1vh;
        padding: 1.2vh 2.8vh;
        cursor: pointer;
        transition: transform 0.25s ease, box-shadow 0.25s ease;
    }

    .toggle-btn:hover {
        transform: translateY(-0.3vh);
        box-shadow: 0 0 15px rgba(230, 135, 233, 0.4);
    }

    .media-card {
        background: #1a1a1a;
        border-left: 4px solid #e687e9;
        border-radius: 1.5vh;
        padding: 3vh;
        margin-bottom: 3vh;
        transition: transform 0.25s ease, box-shadow 0.25s ease;
    }


    .media-card {
        background: #1a1a1a;
        border-left: 4px solid #e687e9;
        border-radius: 1.5vh;
        padding: 3vh;
        margin-bottom: 3vh;
        transition: transform 0.25s ease, box-shadow 0.25s ease;
        WIDTH: 120vh;
        max-width: -webkit-fill-available;
    }

    .media-card:hover {
        transform: translateY(-0.4vh);
        box-shadow: 0 0 20px rgba(230, 135, 233, 0.2);
    }

    .media-title {
        font-size: 2.3vh;
        color: #f1b6f3;
        margin-bottom: 1vh;
        font-weight: 600;
        letter-spacing: 0.5px;
    }

    .media-link {
        color: #e687e9;
        text-decoration: none;
        font-weight: 500;
        font-size: 1.9vh;
        transition: color 0.25s ease;
    }

    .media-link:hover {
        color: #f5a9f7;
        text-decoration: underline;
    }

    .lyrics-box {
        background: #141414;
        border: 1px solid rgba(230, 135, 233, 0.3);
        border-radius: 1vh;
        padding: 2vh;
        margin-top: 1.5vh;
        white-space: pre-wrap;
        color: #f2f2f2;
        font-size: 1.9vh;
        line-height: 1.5;
        box-shadow: inset 0 0 10px rgba(230, 135, 233, 0.1);
        max-height: 30vh;
        overflow-y: auto;
    }


    .loader {
        width: 48px;
        height: 48px;
        border-radius: 50%;
        display: inline-block;
        border-top: 4px solid #FFF;
        border-right: 4px solid transparent;
        box-sizing: border-box;
        animation: rotation 1s linear infinite;
    }

        .loader::after {
            content: '';
            box-sizing: border-box;
            position: absolute;
            left: 0;
            top: 0;
            width: 48px;
            height: 48px;
            border-radius: 50%;
            border-left: 4px solid #e687e9;
            border-bottom: 4px solid transparent;
            animation: rotation 0.5s linear infinite reverse;
        }
    @@keyframes rotation {
        0%

    {
        transform: rotate(0deg);
    }

    100% {
        transform: rotate(360deg);
    }

    }
    }
</style>

</html>
